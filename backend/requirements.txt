fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
ddgs>=9.0.0
beautifulsoup4>=4.12.0
newspaper3k>=0.2.8
httpx>=0.25.0
lxml[html_clean]>=5.0.0
lxml_html_clean
python-multipart>=0.0.20
Pillow>=10.0.0
numpy>=1.26.0
# NOTE: torch, transformers, huggingface_hub removed — too heavy for free-tier hosting (512MB).
# Deepfake detection gracefully returns "Unavailable" without them.
# Re-add if deploying on a server with >=2GB RAM.
